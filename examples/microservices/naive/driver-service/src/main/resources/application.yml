server:
  port: ${SERVER_PORT:8091}

spring:
  application:
    name: driver-service
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/driver_db}
    username: ${DB_USER:driver}
    password: ${DB_PASSWORD:driver}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        jdbc:
          time_zone: UTC
  flyway:
    enabled: true
    locations: classpath:db/migration

# URLs of dependency services (cross-service calls only hit /internal/**).
clients:
  identity:
    url: ${IDENTITY_URL:http://localhost:8081}

# Resilience4j circuit breaker guarding the identity Feign client.
resilience4j:
  circuitbreaker:
    instances:
      identity:
        sliding-window-size: 20
        failure-rate-threshold: 50
        wait-duration-in-open-state: 10s
        permitted-number-of-calls-in-half-open-state: 5
  timelimiter:
    instances:
      identity:
        timeout-duration: 3s

feign:
  circuitbreaker:
    enabled: true

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs
