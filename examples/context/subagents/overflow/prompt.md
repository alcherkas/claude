Use the Task tool to launch the `overflower` subagent with the instruction:
"Read the files in ./filler in order and report each file's MARK code as JSON."

Do NOT read any filler files yourself. When the subagent returns, print its final
message verbatim and nothing else.
