Use the Task tool to launch the `extractor` subagent with the instruction:
"Return the JSON contract for ./filler."

Do NOT read any filler files yourself. Print the subagent's final message verbatim
and nothing else.
